---
title: "Proposal_Figures"
author: "Linnea"
date: "4/20/2020"
output: html_document
---

```{r setup, include=FALSE}

#Function for linear regression of specific conductance vs. chloride
cl.cond.plot <- function(df1, df2, X) {
  d <- df1 %>%
    left_join(df2, by = "date")
  
  ggplot(d, aes(chloride_mgL, sp.cond)) +
    geom_point() + 
    geom_smooth(method = "lm", se = FALSE, color = "#7496D2") +
    labs(y = "Specific Conductivity (µS/cm) @ 25°C\n", 
         x = "\nChloride Concentration (mg/L)") +
    theme(panel.background = element_rect(fill = "white", colour = "white",
                                          size = 2, linetype = "solid"),
          panel.grid.major = element_line(size = 0.25, linetype = 'solid',
                                          colour = "gray88"), 
          panel.grid.minor = element_line(size = 0.25, linetype = 'solid',
                                          colour = "gray88"),
          axis.text = element_text(size =13, face = "bold"),
          axis.title = element_text(size =13, face = "bold"))
}
```

```{r}
YN <- cl.cond.plot(loggerYN, labYN, "YN") 
YI <- cl.cond.plot(loggerYI, labYI, "YI")
YS <- cl.cond.plot(loggerYS, labYS, "YS")
SW <- cl.cond.plot(loggerSW, labSW, "SW")
SMC <- cl.cond.plot(logger6MC, lab6MC, "6MC")
DC <- cl.cond.plot(loggerDC, labDC, "DC")
PBMS <- cl.cond.plot(loggerPBMS, labPBMS, "PBMS")

a <- loggerPBSF %>%
mutate(date = round_date(date, "30 minutes")) 

PBSF <-cl.cond.plot(a, labPBSF, "PBSF")


YN +
  plot_annotation(caption = paste(strwrap("Specific conductivity vs. chloride relationship in the Yahara River at Highway 113. (inlet to Lake Mendota). The linear regression is represented by the equation y=8.6x+360. The positive correlation (r = 0.81) has an r-squared value of 0.66, and a p-value of 0.094."), collapse = "\n"))

SMC + 
  labs(caption = paste(strwrap("Specific conductivity vs. chloride relationship in the Sixmile Creek at Highway M. (inlet to Lake Mendota). The linear regression is represented by the equation y=18x+18. The positive correlation (r = 0.94) has an r2 value of 0.88, and a p-value of 0.018."), collapse = "\n"))

```

